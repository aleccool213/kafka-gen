/**
 * This is run on initial install of the application.
 * 
 * This script:
 * - looks at your local schema registry and its topics
 * - copies those topics to the config in ../src/config/app.json
 */
